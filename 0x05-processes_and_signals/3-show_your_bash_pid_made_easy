#!/usr/bin/env bash
# This script displays the PID along with the process name of processes containing the word "bash".
# Use pgrep command to find PIDs of processes with the word "bash" in their names
pgrep -l bash
